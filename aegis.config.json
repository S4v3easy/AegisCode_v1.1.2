{
  "aegisVersion": "0.1.0",
  "severity": "medium",
  "languages": [
    "JavaScript/TypeScript"
  ],
  "stack": [
    "@inquirer/prompts",
    "@oclif/core",
    "@oclif/plugin-help",
    "@oclif/plugin-plugins",
    "chalk",
    "dotenv",
    "ora",
    "@eslint/compat",
    "@oclif/prettier-config",
    "@oclif/test",
    "@types/chai",
    "@types/mocha",
    "@types/node",
    "chai",
    "eslint",
    "eslint-config-oclif",
    "eslint-config-prettier",
    "mocha",
    "oclif",
    "shx",
    "ts-node",
    "typescript"
  ],
  "ai_rules": [
    "Commands must only handle input parsing/output rendering; business logic must reside in core modules isolated from CLI dependencies.",
    "Core services must expose strictly-typed APIs for plugins to consume, with no direct references to @oclif/core or command-specific code.",
    "All user input validation and CLI output formatting (using chalk/ora) must occur in dedicated utility layers, never in core business logic."
  ]
}